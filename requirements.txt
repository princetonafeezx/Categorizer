# ll-categorizer — pinned-style flat list for CI or `pip install -r requirements.txt`
#
# Preferred installs (editable, uses pyproject.toml):
#   Runtime only:     pip install -e .
#   With dev tools:   pip install -e ".[dev]"

# --- Runtime (see pyproject.toml [project] dependencies) ---
typing-extensions>=4.0.0

# --- Dev / test / lint (see pyproject.toml [project.optional-dependencies] dev) ---
pytest>=7.0.0
mypy
black
flake8

# --- Build (sdist/wheel; see pyproject.toml [build-system] requires) ---
setuptools>=61
wheel
